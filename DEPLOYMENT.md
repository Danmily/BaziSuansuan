# 部署指南

本项目可以部署到多个平台，推荐使用 **Vercel**（最简单快速）或 **Netlify**。

## 🚀 方法一：Vercel 部署（推荐）

### 优点
- ✅ 免费且快速
- ✅ 自动 HTTPS
- ✅ 自动部署（连接 GitHub 后）
- ✅ 全球 CDN 加速
- ✅ 支持自定义域名

### 部署步骤

1. **准备代码**
   ```bash
   # 确保代码已提交到 GitHub
   git add .
   git commit -m "准备部署"
   git push origin main
   ```

2. **访问 Vercel**
   - 打开 https://vercel.com
   - 使用 GitHub 账号登录

3. **导入项目**
   - 点击 "Add New..." → "Project"
   - 选择你的 GitHub 仓库
   - Vercel 会自动检测到这是一个 Vite 项目

4. **配置项目**
   - **Framework Preset**: Vite（自动检测）
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`

5. **部署**
   - 点击 "Deploy"
   - 等待构建完成（约 1-2 分钟）
   - 部署成功后，你会得到一个类似 `https://your-project.vercel.app` 的链接

6. **分享链接**
   - 将链接分享给朋友们即可访问！

### 注意事项
- API Key 需要用户自己在浏览器中设置（存储在 localStorage）
- 不需要配置环境变量（因为 API Key 是用户自己输入的）

---

## 🌐 方法二：Netlify 部署

### 部署步骤

1. **准备代码**
   ```bash
   git add .
   git commit -m "准备部署"
   git push origin main
   ```

2. **访问 Netlify**
   - 打开 https://www.netlify.com
   - 使用 GitHub 账号登录

3. **导入项目**
   - 点击 "Add new site" → "Import an existing project"
   - 选择你的 GitHub 仓库

4. **配置构建设置**
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`

5. **部署**
   - 点击 "Deploy site"
   - 等待构建完成
   - 你会得到一个类似 `https://your-project.netlify.app` 的链接

---

## 📦 方法三：GitHub Pages 部署

### 部署步骤

1. **安装 gh-pages**
   ```bash
   npm install --save-dev gh-pages
   ```

2. **修改 package.json**
   在 `scripts` 中添加：
   ```json
   "deploy": "npm run build && gh-pages -d dist"
   ```

3. **修改 vite.config.ts**
   ```typescript
   import { defineConfig } from 'vite'
   import react from '@vitejs/plugin-react'

   export default defineConfig({
     plugins: [react()],
     base: '/your-repo-name/' // 替换为你的仓库名
   })
   ```

4. **部署**
   ```bash
   npm run deploy
   ```

5. **启用 GitHub Pages**
   - 在 GitHub 仓库设置中
   - 进入 "Pages" 设置
   - Source 选择 "gh-pages" 分支
   - 保存后访问 `https://your-username.github.io/your-repo-name/`

---

## 🔧 本地构建测试

在部署前，建议先在本地测试构建：

```bash
# 安装依赖
npm install

# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

访问 `http://localhost:4173` 查看预览效果。

---

## 📝 部署后注意事项

1. **API Key 配置**
   - 用户需要在网页上点击 "API设置" 按钮
   - 输入自己的 API Key（DeepSeek、OpenAI 等）
   - API Key 存储在浏览器 localStorage，不会上传到服务器

2. **功能说明**
   - 所有功能都可以正常使用
   - AI 分析需要用户自己配置 API Key
   - 八字计算、人生K线等基础功能无需 API Key

3. **分享给朋友**
   - 直接分享部署后的链接即可
   - 每个用户需要自己设置 API Key 才能使用 AI 功能

---

## 🆘 常见问题

### Q: 部署后页面空白？
A: 检查 `vite.config.ts` 中的 `base` 配置是否正确（GitHub Pages 需要配置）

### Q: 构建失败？
A: 检查是否有 TypeScript 错误，运行 `npm run build` 查看详细错误信息

### Q: API 调用失败？
A: 这是正常的，用户需要自己在浏览器中设置 API Key

---

## 🎉 推荐平台对比

| 平台 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **Vercel** | 最快最简单，自动部署 | - | ⭐⭐⭐⭐⭐ |
| **Netlify** | 功能强大，免费额度大 | 配置稍复杂 | ⭐⭐⭐⭐ |
| **GitHub Pages** | 完全免费 | 需要手动部署，配置复杂 | ⭐⭐⭐ |

**建议：优先使用 Vercel，5 分钟即可完成部署！**
